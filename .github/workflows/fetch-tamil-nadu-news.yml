name: Fetch Tamil Nadu News

on:
  schedule:
    # 1:00 AM IST (Asia/Kolkata) = 19:30 UTC
    - cron: "30 19 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch-news:
    runs-on: ubuntu-latest
    # Runtime scales with API totalResults (~10 articles/page, 30 calls then 15 min pause).
    # 964 articles ≈ 55 min; 3000 articles ≈ 2.5 h. Cap at GitHub's 360 min job limit.
    timeout-minutes: 360

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r TN-News/Code/requirements.txt

      - name: Fetch Tamil Nadu news
        env:
          NEWSDATA_API_KEY: ${{ secrets.NEWSDATA_API_KEY }}
        run: python TN-News/Code/fetch_tamil_nadu_news.py

      - name: Commit and push new JSON
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "TN-News/Response JSON/"
          if git diff --staged --quiet; then
            echo "No news JSON changes to commit."
          else
            git commit -m "chore: update Tamil Nadu news $(date -u +%Y-%m-%d)"
            git push
          fi
